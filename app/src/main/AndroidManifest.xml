<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unisinos.db2realm" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".AppPrincipal">
        <activity android:name="br.unisinos.db2realm.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="br.unisinos.db2realm.AddUpdateUsuario" />
        <activity android:name="br.unisinos.db2realm.AddAnuncio" />
        <activity android:name="br.unisinos.db2realm.VisualizarUsuarios" />


    </application>

</manifest>
